name: Canopy

on:
  push:
    branches: [main]

jobs:
  canopy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.CANOPY_PAT }}
      - uses: bruno-portfolio/canopy-code@d24f1e76af3afd0a02ac91b21d718941545a124b
        with:
          token: ${{ secrets.CANOPY_PAT }}
